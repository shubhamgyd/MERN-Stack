var x = 5; // This is a statement
var name = "John"; // Declaring a variable named "name" and assigning it a string value
let age = 30; // Declaring a variable named "age" using let
const pi = 3.14; // Declaring a constant variable named "pi"

// This is a single-line comment

/*
This is a multi-line comment.
It can span multiple lines.
*/

var num = 42; // Number
var text = "Hello, World!"; // String
var isTrue = true; // Boolean
var person = { name: "Alice", age: 25}; // Object
var colors = ["red", "green", "blue"]; // Array

var sum = 5 + 3; // Addition
var isGreaterThan = 10 > 5; // Comparison (greater than)
var isAnd = true && false; // Logical (AND)

console.log(isAnd)